# Normalize line endings. Scripts that run on POSIX must stay LF even when the
# repo is checked out on Windows.
* text=auto eol=lf
*.sh   text eol=lf
*.py   text eol=lf
*.ps1  text eol=crlf
*.png  binary
*.jpg  binary

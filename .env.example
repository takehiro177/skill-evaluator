# Optional — only needed for the deepeval path (agent_tools/deepeval_runner.py).
# Copy to .env and fill in your key. The core evaluation needs none of this.
#
# The deepeval judge backend is pinned to Anthropic/Claude (this project only
# uses Claude), so ANTHROPIC_API_KEY is the ONLY backend credential required.
ANTHROPIC_API_KEY=
